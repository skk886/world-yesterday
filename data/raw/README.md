# Raw snapshots

The deterministic collector writes one candidate snapshot per Asia/Shanghai date as `YYYY-MM-DD.json`. Category and topic labels are derived from each article's title and RSS description; publisher-wide section lists are not copied into candidates.
