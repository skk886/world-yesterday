# Local usage logs

Per-run JSONL usage logs are local-only and ignored by Git. After every validated edition, the controller regenerates a committed `YYYY-MM.json` report with candidate, page, search, verification, rejection, and measured-token totals.
